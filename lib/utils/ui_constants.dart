import 'package:flutter/material.dart';

const double kSpacing8 = 8.0;
const double kSpacing16 = 16.0;
const double kSpacing24 = 24.0;

const double kFieldRadius = 12.0;
const double kCardRadius = 16.0;

const BorderRadius kCircularBorder12 = BorderRadius.all(
  Radius.circular(kFieldRadius),
);

const BorderRadius kCircularBorder16 = BorderRadius.all(
  Radius.circular(kCardRadius),
);

